import { useState } from "react";
import AppHeader from "../../components/app_header/app_header";
import BottomNav from "../../components/bottom_nav/bottom_nav";
import "./nagrody.css";

export default function Nagrody() {
  const [rewards] = useState([
    {
      id: 1,
      title: "Zasadź Drzewo",
      description: "Przeznacz swoje punkty na posadzenie prawdziwego drzewa!",
      points: "150 pkt",
      isSpotlight: true,
      badge: "POPULARNE",
      icon: "🌱",
    },
    {
      id: 2,
      title: "Darmowa Kawa",
      description:
        "Wymień punkty na kubek ulubionej kawy w lokalnej kawiarni partnerskiej.",
      points: "50 pkt",
      isSpotlight: false,
      badge: "DOSTĘPNE",
      icon: "☕",
    },
  ]);

  return (
    <div className="nagrody-page">
      <AppHeader />
      <main className="nagrody-main-content">
        {/* Hero Section */}
        <section className="hero-section">
          <div className="hero-title-container">
            <h2 className="hero-title">1250</h2>
            <span className="hero-title-label">zwrotów</span>
          </div>
          <div className="hero-content">
            <p className="hero-description">
              Każdy zwrot to nie tylko zwrot do kasy, to inwestycja w lepszy
              świat. Zbieraj punkty i wymień je na nagrody lub wspomóż środowisko.
            </p>
            <div className="hero-card">
              <div className="hero-icon">🌍</div>
              <div className="hero-info">
                <p className="hero-label">TWÓJ WKŁAD</p>
                <p className="hero-value">+250</p>
              </div>
            </div>
          </div>
        </section>

        {/* Rewards Section */}
        <section className="rewards-section">
          <h3 className="rewards-title">Kolekcja Nagród</h3>
          <div className="rewards-container">
            {rewards.map((reward) => (
              <article
                key={reward.id}
                className={`reward-card ${reward.isSpotlight ? "spotlight" : ""}`}
              >
                <div className="reward-badge">{reward.badge}</div>
                <div className="reward-content">
                  <div className="reward-icon">{reward.icon}</div>
                  <h4 className="reward-title">{reward.title}</h4>
                  <p className="reward-description">{reward.description}</p>
                </div>
                <div className="reward-footer">
                  <span className="reward-points">{reward.points}</span>
                  <button className="reward-button">Wymień</button>
                </div>
              </article>
            ))}
          </div>
        </section>
      </main>
      <BottomNav />
    </div>
  );
}
